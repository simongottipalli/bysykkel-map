import { Pin } from "@vis.gl/react-google-maps";

const BysykkelPin = () => {
    return (
        <Pin
            background={'#0425fb'} glyphColor={'#ffffff'} borderColor={'#000'}
        />
    );
}

export default BysykkelPin;